#!/usr/bin/env python

__author__ = 'jpercent'

from setuptools import setup, find_packages

setup(
    name = 'backend-python-prompt',
    version = '0.0.1',
    description = 'Prompt Backend',
    author = 'james@shift5.net',
    packages = ['prompt']
)
